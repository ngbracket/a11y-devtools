name: Release

# Publishes the current package.json version to npm via OIDC trusted publishing
# (no token/OTP). Trigger manually once the version has been bumped + merged.
on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # OIDC token for npm trusted publishing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24.19.0
          # No registry-url: it writes a token-auth .npmrc (NODE_AUTH_TOKEN)
          # that suppresses OIDC. Trusted publishing uses the id-token below.
      - run: npm install -g npm@latest # trusted publishing needs npm >= 11.5.1
      - run: npm --version
      - run: npm ci
      - run: npm run build
      - run: npm test
      - run: npm publish
